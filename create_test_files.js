const fs = require("fs"); const path = require("path"); const userData = path.join(process.env.HOME, "Library/Application Support/electron-sample-app"); const files = ["categories.json", "tags.json", "selected-category.json", "selected-tags.json"]; files.forEach(file => { const filePath = path.join(userData, file); const content = file.includes("category") ? JSON.stringify("all") : JSON.stringify([]); fs.writeFileSync(filePath, content); console.log(`Created ${filePath}`); });
